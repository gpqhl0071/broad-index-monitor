# 国内宽指基金行情 · 使用说明

本地 Web 页面，展示上证 / 深证 / 科创宽基指数与 ETF 的实时行情，每 **10 秒** 自动刷新。

---

## 一、环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | macOS / Linux（Windows 可用 WSL） |
| Python | 3.9 及以上 |
| 网络 | 能访问东方财富、新浪、腾讯等公开行情接口 |

首次运行脚本会自动创建虚拟环境 `.venv` 并安装依赖，无需手动 `pip install`。

---

## 二、一键脚本（推荐）

在项目目录 `broad-index-monitor` 下执行：

```bash
chmod +x start.sh stop.sh restart.sh
```

| 脚本 | 作用 | 示例 |
|------|------|------|
| `./start.sh` | **后台**启动服务 | 适合日常挂机看行情 |
| `./start.sh -f` | **前台**启动（改代码自动重载） | 适合开发调试 |
| `./stop.sh` | 停止服务 | 释放 8765 端口 |
| `./restart.sh` | 先停后启 | 改配置或异常后恢复 |

启动成功后，浏览器打开：**http://127.0.0.1:8765**

### 自定义端口 / 监听地址

```bash
HOST=0.0.0.0 PORT=9000 ./start.sh   # 局域网可访问
PORT=9000 ./stop.sh                  # 停止时需与启动端口一致
```

### 查看日志

后台模式日志在 `logs/server.log`：

```bash
tail -f logs/server.log
```

进程 PID 记录在 `.run/server.pid`。

---

## 三、页面说明

### 顶部状态栏

- **上次更新**：后端最近一次成功拉取行情的时间
- **刷新间隔**：固定 10 秒
- **上游异常**：某一数据源暂时失败时会提示（会自动切换备用源，多数情况可忽略）
- **成功 / 失败次数**：本次打开页面后的请求统计

### 表格字段

| 列名 | 含义 |
|------|------|
| 跟踪指数 | 该 ETF 跟踪的宽基指数名称 |
| 基金简称 | 场内基金名称 |
| 代码 | 6 位证券代码 |
| 涨跌幅 | 相对昨收的百分比（红涨绿跌） |
| 最新价 | 当前价 |
| 涨跌额 | 相对昨收的绝对变动 |
| 今开 / 昨收 | 开盘价、前收盘价 |
| 成交额 | 当日成交金额 |
| 振幅 | 当日最高最低价差相对昨收的比例 |

数据按 **上证 → 深证 → 科创** 分组展示。

---

## 四、常见问题

### 1. 页面显示「上游异常」或失败次数较多

公开接口有频率限制或短暂断连，服务会**轮流**使用东方财富、新浪、腾讯三家源，并自动 failover。可尝试：

```bash
./restart.sh
```

若仍频繁失败，检查本机网络、代理设置，或稍后再试（非交易时段部分字段可能为空）。

### 2. 端口已被占用

```bash
./stop.sh
./start.sh
```

或查看占用：`lsof -i :8765`

### 3. 如何增减展示的基金

编辑 `backend/config.py` 中的 `SH_ITEMS`、`SZ_ITEMS`、`KC_ITEMS`，保存后执行 `./restart.sh`。

### 4. 交易时段外数据异常

收盘后部分字段可能为昨收或为空，属正常现象，仅供个人参考，**非官方行情**。

---

## 五、手动启动（可选）

不通过脚本时：

```bash
cd broad-index-monitor
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn backend.main:app --host 127.0.0.1 --port 8765
```

---

## 六、API 接口（进阶）

| 路径 | 说明 |
|------|------|
| `GET /` | 行情页面 |
| `GET /api/quotes` | 当前缓存行情 JSON |
| `GET /api/health` | 健康检查 |
| `GET /api/weather?lat=&lon=` | 天气（可选坐标） |

---

## 七、免责声明

数据来自第三方公开接口，仅供个人学习参考，不构成投资建议。请勿用于实盘交易决策的唯一依据。

更多技术细节见 [README.md](./README.md)。
